os: linux
sudo: required
dist: trusty
branches:
  except:
    - av/*
env:
  - PATH=$PATH:/usr/local/opt/qt5/bin
#before_script:
#- brew update > /dev/null
#- brew install qt5
script:
#- qmake QtTest.pro
#- make
  - sudo apt-get install -fy libavformat-dev libswscale-dev
  - ls -l /usr/include

