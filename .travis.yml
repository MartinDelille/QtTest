language: cpp

os:
  - linux

compiler:
  - clang


install:
  - cd rtmidi
  - sudo apt-get -qy install autoconf libtool
  - libtoolize
  - automake --add-missing
  - autoconf

notifications:
  email:
    on_success: always
