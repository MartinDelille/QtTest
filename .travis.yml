os: linux
branches:
  except:
    - av/*
env:
  - PATH=$PATH:/usr/local/opt/qt5/bin
#before_script:
#- brew update > /dev/null
#- brew install qt5
script:
#- qmake QtTest.pro
#- make
  - sudo apt-get install -y libavcodec53 libavformat53 libavcodec-dev libavformat-dev libswscale-dev
  - ls -l /usr/include

